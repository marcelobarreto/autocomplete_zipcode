require 'spec_helper'

describe AutocompleteZicode do
  it 'has a version number' do
    expect(AutocompleteZicode::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
